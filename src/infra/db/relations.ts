import { provinceRelations } from './schema/province'
import { RefreshTokenRelation } from './schema/refresh-token'
import { storyRelations } from './schema/story'

export const relations = {
  RefreshTokenRelation,
  storyRelations,
  provinceRelations,
}
